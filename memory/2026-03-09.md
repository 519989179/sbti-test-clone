# 2026-03-09 工作日志

## 今日配置与系统整理

### 模型统一
- 按老大要求，把运行中的当前活跃会话模型切到：`vapi/gpt-5.4-2026-03-05`
- 同时把新会话默认模型也改成：`vapi/gpt-5.4-2026-03-05`
- 之前默认模型是：`ccodezh/gpt-5.4-2026-03-05`

### Feishu 插件告警排查
- 发现告警含义：不是飞书坏了，而是 `feishu` 插件出现重复声明/重复加载提示。
- 定位到来源与 `~/.openclaw/extensions/feishu/index.ts` 相关。
- 已做配置清理：
  - 删除重复的 `plugins.entries.feishu`
  - 设置 `plugins.allow = ["feishu"]`
- 当前 `openclaw.json` 已规范成只显式允许一份 `feishu` 插件。
- 但 CLI 输出里仍持续出现旧的 duplicate warning，说明还需要进一步确认是否是网关/缓存/插件发现机制残留导致。

### 记忆体系建设
- 老大明确要求：把记忆体系建立完整。
- 已补齐/更新：
  - `MEMORY.md`
  - `USER.md`
  - 本日志 `memory/2026-03-09.md`
- 记住的关键偏好：
  - 小技术问题自己解决，不要轻易打扰老大
  - 回复直接，少废话
  - 不要把内部工具噪音暴露给老大
  - 长期要靠文件记忆，不能只靠会话上下文

### 从《卷卷工作手册》同步到长期记忆的要点
- 任务管理要更透明：适合随时简短汇报“在做什么 / 做到哪了 / 还差什么”。
- 相同类型的工作要沉淀为标准流程，减少重复犯错。
- 重要工作后要主动更新本地记忆与相关手册，而不是等提醒。
- 对方向不明确的事项要确认；对小技术问题优先自行解决。
- 旧文档中的表达风格要求（如固定前缀、过于活泼）低于老大最新明确偏好；执行时以最新偏好为准。

### 上下文窗口诉求
- 老大明确表示：**200k 太小，希望临时上下文更大**。
- 已把配置中的模型声明上调到：
  - `contextWindow = 1000000`
  - `maxTokens = 64000`
- 但老大已明确：上下文是否真实生效这件事，当前阶段先不继续验证。

## 当前结论
- 记忆体系已建立完成，并且已经把《卷卷工作手册》的关键规则同步进长期记忆。
- 后续要持续做的是：每次重要工作后更新记忆，避免长期记忆再次空转。
