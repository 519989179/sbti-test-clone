# 2026-03-12

## 今日工作日志（整理版）

### 一、今天做了哪些工作
- 确认并固化了图片模型使用规则：
  - **图片生成默认统一使用 5.0 模型**。
  - 仅当艾乐明确要求做版本对比，或明确指定其他模型时，才临时切换。
  - 若艾乐明确要求以后默认切换模型，需要同步更新长期记忆和工作文档。
- 实操跑通了即梦 / Seedream 出图链路，并已验证可以通过飞书接口把生成图片直接发回当前私聊。
- 验证了 **Seedream 5.0 生成 21:9 横版公众号封面** 的能力，可用于公众号封面场景确认。
- 接到“艾乐同学”公众号新任务，目标包含：**写稿、生成 5.0 封面、公众号排版、送入草稿箱**。
- 排查了公众号草稿箱链路的本地现状：
  - workspace 内存在历史草稿样例：`wechat-pub/first_draft.json`
  - 本机存在公众号发布相关实现：`~/.agent-reach/tools/arcs-mcp/extension/wechat/`
  - 同时确认当前环境里公众号 API 所需配置尚不完整，Chrome 默认资料也未保留公众号后台登录态
- 接收并解压了艾乐补发的“公众号运营技能包”，已确认其中包含核心文件：
  - `publish_aile_article.py`
  - `skills/wxmp-skills/SKILL.md`
  - `skills/wxmp-skills/publisher.py`
- 确认后续“艾乐同学”公众号运营应优先按这套 **wxmp skill** 执行。
- 接收并记录了公众号 `AppID`、`AppSecret`，以及微信官方群发 API 文档链接。
- 安装并验证了 `nano-banana-pro` skill，实际跑通了 **`gemini-3.1-flash-image-preview`** 图片编辑能力。
- 新增并固化图片修改规则：**图片编辑 / 改字 / 局部修改默认使用 `gemini-3.1-flash-image-preview`，不再默认用即梦 5.0。**

### 二、今天碰到的问题
- **公众号送草稿箱链路没有在白天跑通。**
- 当前阻塞点主要有两个：
  - 环境变量中的公众号 API 相关配置不完整（如 `WECHAT_PUBLIC_ACCOUNT_APPID/SECRET`、`WECHAT_COVER_IMAGE` 等曾为空或待补齐）
  - 本机 Chrome 默认资料未保留公众号后台登录态，导致基于现有本地能力直接推进存在不确定性
- 另外，虽然已经拿到公众号技能包和 API 文档，但从“资料齐全”到“稳定送入草稿箱”之间，还缺少一次完整闭环验证。

### 三、以后怎么避免类似问题
- **先跑通闭环，再做优化。** 对这类“写稿 + 排版 + 平台入库”的链路，优先第一时间验证最小可行路径，不要等到内容都准备完才发现投递链路卡住。
- **平台接入前先做环境检查清单。** 至少提前确认：
  - AppID / AppSecret 是否可用
  - 是否有封面图默认配置
  - 登录态是否保留
  - 本地脚本依赖、权限、样例文件是否齐全
- **技能包一到手就先验证关键入口脚本。** 不只确认文件存在，还要尽早验证核心脚本能否真正完成一次草稿写入。
- **把模型默认规则文档化。** 今天已经明确：
  - 生成图 → 默认 5.0
  - 改图 / 编辑图 → 默认 `gemini-3.1-flash-image-preview`
  后续继续保持写入文档和记忆，避免来回反复确认。

### 四、下一步计划
- 继续推进“艾乐同学”公众号首篇内容的完整闭环：
  1. 明确文章主题与成稿内容
  2. 生成并确认封面 / 配图
  3. 按公众号样式完成排版
  4. 跑通送入草稿箱
- 基于今天拿到的 wxmp skill，优先验证 `publish_aile_article.py` / `publisher.py` 的实际可用性。
- 若 API 链路仍不稳定，需要尽快判断是否切换为更稳妥的备选方案（例如人工登录态辅助或改用另一条发布实现）。

### 五、建议
- 建议把“公众号发布前置检查”沉淀成固定 checklist，后面每次做公众号内容时先过一遍，减少半路卡壳。
- 建议首篇公众号文章以“**先成功入草稿箱**”为唯一核心目标，标题、配图、排版风格都可以在闭环跑通后再迭代。
- 建议后续把公众号链路跑通后，顺手补一份本地操作说明和故障排查笔记，避免未来重复踩坑。

### 六、哪些需要艾乐拍板
- 如果当前 API / 登录态链路继续不稳，**是否允许临时采用更直接但人工参与更高的备选方案** 来先把草稿箱闭环跑通。
- 首篇公众号内容的最终主题、口径和发布时间优先级，如需我继续深挖某个选题，最好尽快定方向。
- 如果后续公众号默认流程要正式切到 wxmp skill，是否需要我同步把这套流程固化成长期标准工作流文档。

### 七、明天的优先级
1. **最高优先级：** 跑通“艾乐同学”公众号文章送入草稿箱的完整闭环。
2. 验证 wxmp skill 的关键发布脚本是否可稳定使用。
3. 完成首篇公众号内容的正文、封面、配图、排版整合。
4. 补充公众号发布前置检查清单与踩坑记录。
5. 如闭环成功，准备后续数据跟踪与内容迭代节奏。

## 今日记录（原始要点）
- 老大明确确认：**以后所有图片生成都必须使用 5.0 模型**。
- 已实操跑通即梦/Seedream 出图，并通过飞书接口把生成图片直接发回当前私聊。
- 当前规则：除非老大明确要求做版本对比或指定别的模型，否则默认不要再用 4.0 做图片生成。
- 老大补充确认：如果他临时指定其他模型，就按当次指定执行；如果他明确说以后默认切换图片模型，要同步更新长期记忆和工作文档。
- 已验证 Seedream 5.0 可正常生成 **21:9** 横版公众号封面，并通过飞书接口直接发回当前私聊用于确认。
- 今天新增主任务：为“艾乐同学”公众号完成一篇内容，包含：写稿、5.0 封面、公众号排版、送入草稿箱。
- 当前已排查到公众号草稿箱链路的本地现状：workspace 内有历史草稿样例 `wechat-pub/first_draft.json`；本机还存在 `~/.agent-reach/tools/arcs-mcp/extension/wechat/` 的公众号发布实现，但当前环境变量里的公众号 API 配置（`WECHAT_PUBLIC_ACCOUNT_APPID/SECRET`、`WECHAT_COVER_IMAGE` 等）为空，Chrome 默认资料也未保持公众号后台登录态。
- 老大补发了“公众号运营技能包”，已解压确认其中包含 `publish_aile_article.py`、`skills/wxmp-skills/SKILL.md`、`skills/wxmp-skills/publisher.py` 等文件；后续“艾乐同学”公众号运营默认应按这套 skill 执行。
- 老大重新提供了公众号 `AppID` 与 `AppSecret`，并补充发送了微信官方群发 API 文档链接。
- 这次未能在白天把公众号文章送入草稿箱，老大已要求：**先暂停，等晚上有空时继续干这个活**。
- 已安装并验证 `nano-banana-pro` skill；实际通过老大提供的中转接口与模型名，成功跑通 **`gemini-3.1-flash-image-preview`** 图片编辑。
- 新规则生效：**图片生成默认继续用 5.0；但图片修改/改图一律默认用 `gemini-3.1-flash-image-preview`，不要再默认用即梦 5.0。**
