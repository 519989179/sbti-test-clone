# 2026-03-13

- 继续排查“艾乐同学”公众号送入草稿箱链路。
- 已实测新增本地检查脚本：`wechat-pub/check_wechat_pipeline.py`。
- 当前实测结果：
  - `~/.agent-reach/tools/arcs-mcp/.env` 中仍缺 `WECHAT_PUBLIC_ACCOUNT_APPID`、`WECHAT_PUBLIC_ACCOUNT_SECRET`
  - `WECHAT_COVER_IMAGE` 仍为空
  - `browser_user_dir/Default/Cookies` 虽存在，但未检出 `mp.weixin.qq.com / weixin.qq.com` 登录 Cookie
- 结论：当前未真正卡在“文章内容”，而是卡在**公众号开发者配置 + 登录态都未就绪**，因此还不能直接跑通 `draft/add`。
- 已补充文档：`wechat-pub/README.md`，用于快速定位阻塞点与后续打通步骤。
- 下午艾乐补发了公众号 `AppID / AppSecret`；我已写入 `~/.agent-reach/tools/arcs-mcp/.env` 并实测 `stable_token` 获取成功，说明**公众号开发者接口已打通**。
- 当前公众号链路的新阻塞点收敛为：`WECHAT_COVER_IMAGE` 仍为空，需先补封面素材 `media_id`，再继续打 `draft/add` 草稿箱接口。
- 按艾乐要求，今天重新确认并再次踩实图片模型规则：
  - **新图片生成默认一律用 5.0**
  - **已有图片修改 / 改字 / 局部编辑默认用 `gemini-3.1-flash-image-preview`**
  以后不要再在新出图任务上跑偏到 Gemini 生成链路。
- 已按艾乐要求用 **安全模式**执行 `agent-reach install --env=auto --safe`，并跑了 `agent-reach doctor`；当前状态为 **6/13 渠道可用**。
- 艾乐后续发来一张海报图，要求把主题改成“广州龙虾节爆火”；我先用本地 PIL 方案快速生成了一版改图：`tmp/guangzhou-lobster-poster-edit.jpg`。
- 晚上重新确认并最终跑通了**Gemini 改图真实可用链路**：
  - 中转：`https://yunwu.ai`
  - 模型：`gemini-3.1-flash-image-preview`
  - 接口：`POST /v1beta/models/gemini-3.1-flash-image-preview:generateContent?key=<API_KEY>`
  - 方式：原图转 base64 放进 `inline_data`，编辑指令放 `text`，`responseModalities` 设为 `TEXT + IMAGE`
  - 返回：从 `candidates[0].content.parts[*].inlineData.data` 取回图片并保存
- 今天已实际用这条链路完成两次改图：
  1. `淄博赛博龙虾节` → `广州喷泉晚会`
  2. `广州喷泉晚会` → `淄博喷泉交流会`
- 艾乐明确要求：这类流程不能只记在当日日志里，必须同步写进**长期记忆 + 工作手册 / TOOLS.md**，避免以后再次忘记。
